spring.application.name=mini-erp

# Configuracoes do PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/mini-erp
spring.datasource.username=postgres
spring.datasource.password=root
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuracoes do JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# Configuracoes de Seguranca (temporaria)
#spring.security.user.name=admin
#spring.security.user.password={noop}admin

# Configuracoes JWT
jwt.secret=ZGV2ZWxvcGVyLWdvbGRlbi1lcnAtand0LXNlY3JldC1rZXktZm9yLWF1dGhlbnRpY2F0aW9uLXB1cnBvc2VzLW9ubHk=
jwt.expiration=86400

# Configuracoes do Feign Client
feign.client.config.default.connect-timeout=5000
feign.client.config.default.read-timeout=10000

# Configuracoes de validacao
spring.jackson.deserialization.fail-on-unknown-properties=false
